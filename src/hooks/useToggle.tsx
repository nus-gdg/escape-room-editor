// import React, {useCallback, useContext, useState} from "react";
import Root2 from "../common/containers/Root2";
// import {isEmpty} from "lodash";
//
// export default function useToggle<T>(initialState: T) {
//     const [toggles, setToggles] = useState<T>(initialState);
//
//     const handleToggle = useCallback(() => {
//         setState(state => ({
//             ...state,
//             ...{showRooms: !state.showRooms}
//         }));
//     }, []);
//
//     if (isEmpty(ctx)) {
//         throw new Error("useRoot must be used within RootStoreProvider")
//     }
//     return ctx;
// }
