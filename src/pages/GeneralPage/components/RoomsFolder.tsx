import React from "react";
// import {Room} from "../../../state/data/data";
// import RoomFolder from "./RoomFolder";
//
// interface FolderMenuState {
//     showRooms: boolean,
//     showItems: boolean,
// }
//
// interface RoomsFolderProps {
//     className?: string,
//     rooms?: Room[];
// }
//
// const FolderMenu = (
//     {
//         className = "",
//         rooms = [],
//     }: RoomsFolderProps) => {
//     console.log("RoomsFolder: Rendered");
//
//     const hasRooms = rooms && rooms.length > 0;
//     return (
//         <div style={{display: "flex", flexDirection: "column"}}>
//             {hasRooms && rooms?.map(room => {
//                 return <RoomFolder key={room.id} room={room}/>;
//             })}
//         </div>
//     );
// };
//
//
// const FolderMenu = (
//     {
//         className = "",
//         rooms = [],
//     }: RoomsFolderProps) => {
//     console.log("RoomsFolder: Rendered");
//
//     const hasRooms = rooms && rooms.length > 0;
//     return (
//         <div style={{display: "flex", flexDirection: "column"}}>
//             {hasRooms && rooms?.map(room => {
//                 return <RoomFolder key={room.id} room={room}/>;
//             })}
//         </div>
//     );
// };
//
// export default React.memo(FolderMenu);
